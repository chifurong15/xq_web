(function(window, angular) {
    'use strict';
    angular
        .module("app")
        .controller(
            'weChatMag',
            [
                '$localStorage',
                '$scope',
                '$location',
                '$log',
                '$q',
                '$rootScope',
                '$window',
                'routeService',
                '$http',
                '$ajaxhttp',
                'moduleService',
                function weChatMag($localStorage, $scope,
                                        $location, $log, $q, $rootScope, $window,
                                        routeService, $http, $ajaxhttp, moduleService) {





                } ]);
})(window, angular);
